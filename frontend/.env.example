# ── API Base URL ──────────────────────────────────────────────────────────────
# Local dev (direct to backend):
VITE_API_URL=http://localhost:5000

# Docker / production:
# Leave empty — nginx proxies /api/* to the backend container automatically
# VITE_API_URL=
